enum AnswerType {
  shortText,
  multipleChoice,
  singleChoice,
  contactInfo,
  address,
  phoneNumber,
  longText,
  yesNo,
  shortNumber,
}

enum ImageLayout {
  inBetween,
  right,
  left,
  rightExpanded,
  leftExpanded,
  background
}
