enum AnswerType {
  shortText,
  multipleChoice,
  singleChoice,
  contactInfo,
  shortContactInfo,
  address,
  phoneNumber,
  longText,
  yesNo,
  shortNumber,
}

enum ImageLayout {
  inBetween,
  right,
  left,
  rightExpanded,
  leftExpanded,
  background
}
